// the pop methods removes the last element of an array. and returns the removed elements.
// and this method is changed original array

const arr = ["abu musa", "Omar Faruq", "Usman", "Ali", "Abu Hurayra"];
console.log(arr.pop(0));
